# Dormant Pages

These pages are intentionally kept out of the active route table.

They are not part of the current production information architecture, but they remain in the repo because they may be revived later:

- `HomePage.tsx`
- `ProjectsPage.tsx`
- `WorkPage.tsx`
- `LibraryPage.tsx`

If one of these pages returns, bring it back completely:

- add the route
- update navigation
- update tests
- update docs
